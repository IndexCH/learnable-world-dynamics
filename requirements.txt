# ==========================================
# Learnable World Dynamics - 依赖清单
# ==========================================

# 1. 核心数值计算 (数据生成与处理)
numpy>=1.24.0

# 2. 深度学习框架 (构建大脑)
# 注意：这里只写通用版本限制，具体安装建议看下方 GPU 说明
torch>=2.0.0

# 3. 辅助与可视化工具
matplotlib>=3.7.0  # Phase 1 之后画 Rollout 轨迹图必备
tqdm>=4.65.0       # 训练时的进度条神器，看着 Loss 下降很解压

# 4. 可选：交互式调试 (强烈推荐)
jupyter>=1.0.0     # 方便后续以 Notebook 形式逐个单元格测试模型